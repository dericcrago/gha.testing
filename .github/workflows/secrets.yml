---
name: secrets
on: push
jobs:
  secrets:
    name: show the secrets
    runs-on: ubuntu-18.04
    steps:
      - name: echo the secrets
        env:
          FOO: ${{ secrets.FOO }}
        run: |
          echo "FOO is set to bar"
          echo "FOO is set to ${FOO}"

      - name: echo the secrets
        run: |
          echo "FOO is set to bar"
          echo "FOO is set to ${FOO}"
